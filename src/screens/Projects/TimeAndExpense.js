import { StyleSheet, Text, View } from "react-native";
import React from "react";

const TimeAndExpense = () => {
  return (
    <View>
      <Text>TimeAndExpense</Text>
    </View>
  );
};

export default TimeAndExpense;

const styles = StyleSheet.create({});
