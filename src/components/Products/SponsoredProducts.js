import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const SponsoredProducts = () => {
  return (
    <View>
      <Text>SponsoredProducts</Text>
    </View>
  )
}

export default SponsoredProducts

const styles = StyleSheet.create({})